import React from 'react'

const HeroSection = (imagesrc, title, alttext) => {
  return (
    <div>
      {/* <img src={imagesrc} alt={alttext} /> */}
      <h2>{title}</h2>
      Hero
    </div>
  )
}

export default HeroSection