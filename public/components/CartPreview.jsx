import React from 'react'

const CartPreview = () => {
  return (
    <div>
        <button onClick={() => navigate("/shop")}>
            View All
        </button>
    </div>
  )
}

export default CartPreview